#!/bin/bash
# This hook is run after every virtualenv is activated.

my_env=`basename $VIRTUAL_ENV`
echo Setting DJANGO_SETTINGS_MODULE to $my_env.settings
export DJANGO_SETTINGS_MODULE=$my_env.settings
cd $VIRTUAL_ENV/src
